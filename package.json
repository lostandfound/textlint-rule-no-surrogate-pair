{
  "version": "1.0.0",
  "keywords": [
    "textlintrule"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/",
    "src/"
  ],
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build"
  },
  "devDependencies": {
    "textlint-scripts": "^1.2.2"
  },
  "name": "textlint-rule-no-surrogate-pair",
  "directories": {
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lostandfound/textlint-rule-no-surrogate-pair.git"
  },
  "author": "Hiroshi Takase",
  "license": "MIT",
  "description": "Detects surrogate pairs in sentences."
}
